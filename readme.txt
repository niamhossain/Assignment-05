1️⃣ What is the difference between var, let, and const?
   = can't reassign const but can reassign the other two.
2️⃣ What is the spread operator (...)?
   = seperate elements individuall from array/obect
3️⃣ What is the difference between map(), filter(), and forEach()?
   = map() transforms every element, filter() sorts out elements and forEach() can be used for various reason as it iterates over every element
4️⃣ What is an arrow function?
   = basically function with less code
5️⃣ What are template literals?
   =  a way to get variables inside strings using `` and ${}
   